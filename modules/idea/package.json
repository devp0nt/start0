{
  "name": "@/idea",
  "private": true,
  "type": "module",
  "dependencies": {
    "@/backend": "workspace:*",
    "@/lib": "workspace:*",
    "@/prisma": "workspace:*",
    "@/site": "workspace:*"
  },
  "scripts": {
    "types": "rimraf dist && tsc"
  }
}
