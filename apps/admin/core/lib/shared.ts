// import { useResourceParams } from '@refinedev/core'
// import _ from 'lodash'

export const useResourceTitle = (defaultValue?: string) => {
  return undefined as string | undefined
  // const resourceParams = useResourceParams()
  // const name = resourceParams.resource?.name
  // if (!name) {
  //   return defaultValue
  // }
  // const niceName = _.words(name)
  //   .map((word) => _.capitalize(word))
  //   .join(' ')
  // const action = resourceParams.action
  // if (!action) {
  //   return niceName
  // }
  // return `${_.capitalize(action)} ${niceName} `
}
