workspace:
  projects:
    backend:
      source: "apps/backend"
      type: application
      dependsOn: []
    site:
      source: "apps/site"
      type: application
      dependsOn: []
    prisma:
      source: "modules/prisma"
      type: library
      dependsOn: []
    lib:
      source: "modules/lib"
      type: library
      dependsOn: []

typescript:
  syncProjectReferences: true
  syncProjectReferencesToPaths: true
  rootOptionsConfigFileName: "tsconfig.options.json"
  rootConfigFileName: "tsconfig.projects.json"
