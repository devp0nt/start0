{
  "name": "@tools/shared",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "tsgo --noEmit ./tsconfig.json",
    "build": "tsgo --build ./tsconfig.json",
    "watch": "tsgo --watch ./tsconfig.json",
    "clean": "rimraf ./../node_modules/.cache/tsbuildinfo/@tools/shared && rimraf ./dist",
    "build:clean": "rimraf ./../node_modules/.cache/tsbuildinfo/@tools/shared && rimraf ./dist"
  },
  "dependencies": {}
}
