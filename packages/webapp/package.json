{
  "name": "@svagatron/webapp",
  "scripts": {
    "dev": "vite",
    "test": "echo \"Not tests, and it is okay.\"",
    "test_": "dotenv -e .env.test -e .env jest",
    "build": "vite build",
    "lint": "eslint --color --cache --cache-location ./node_modules/.cache/.eslintcache .",
    "lint-fix": "pnpm lint --fix",
    "types": "tsc --noEmit",
    "prettify": "svag-prettier prettify",
    "uinity": "uinity"
  },
  "dependencies": {
    "@svagatron/backend": "workspace:*",
    "@svagatron/general": "workspace:*"
  },
  "type": "module",
  "devDependencies": {}
}
