Функции которые прнимают ctx, оборачивать в withCtx(extends, fn), и тут же генерировать createWithCtx который также может принимать extends

move react route pages itslef to modules

logger client
processes → ?bin

env
prisma
route0

entity Class
enum Class

genreator route files, search fiels and get exports by names

TODO: restart frontend server on code change
refactor lib0

biome rules
biome hotkeys


just
better-auth
https://dev.to/ayoubphy/step-by-step-guide-setting-up-trpc-better-auth-prisma-and-react-router-v7-4ho
plop

full hono route
Хоно модель должна в себе иметь метод и путь, в то же время вроде в ней вообще нет нужды
А нужда есть наверняка в именно инпуте

form
zod numer as text in form with coerce

html title dynamic


@ index files watcher
cron/scheduler
kafka/rabbitmq/pgboss

test unit
test integration trpc
test integration hono
test e2e

sentry
swagger

refine-antd
watch-gen boilerplate
ограничение импортов

hono define route with my schema

devops

В свою админку вшить генератор админки свой без всяких там рефайнов, тупо определять какой формой какие поля редактируются

remove old